<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification Code</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            width: 100vw;
            height: 100vh; /* Ensure full height in mobile browsers */
        }
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8ff;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .container {
            background-color: #ffffff;
            border: 1px solid #d1e7fd;
            width: 100%; /* Adjusted for better mobile responsiveness */
            padding: 0;
            text-align: center;
            box-sizing: border-box;
            position: relative; /* Required for full height */
            height: auto; /* Adjusted to auto for flexibility */
        }
        .header {
            background-color: #87ceeb;
            padding: 0;
            color: #ffffff;
            font-size: 24px;
            font-weight: bold;
        }
        .content {
            margin: 20px 0;
            font-size: 18px;
        }
        .verification-code {
            background-color: #e0f7fa;
            border: 1px dashed #87ceeb;
            display: inline-block;
            padding: 10px 20px;
            font-size: 24px;
            color: #333;
            font-weight: bold;
            cursor: pointer;
        }
        .footer {
            margin-top: 20px;
            font-size: 14px;
            color: #999;
        }
        .byline {
            font-size: 12px;
            position: absolute;
            bottom: 20px;
            left: 0;
            right: 0;
            color: #ccc;
        }
        /* Responsive adjustments */
        @media (max-width: 600px) {
            .header, .content, .verification-code {
                font-size: 20px;
                padding: 15px;
            }
            .container {
                padding: 0;
                height: 100%; /* Full height on mobile */
            }
            .container {
                box-shadow: unset;
                padding: 0;
            }
        }
    </style>
    <script>
        function copyToClipboard() {
            const codeElement = document.getElementById('verificationCode');
            const code = codeElement.textContent;
            navigator.clipboard.writeText(code).then(function() {
                // Change background color temporarily to indicate copy success
                codeElement.style.backgroundColor = "#b2fab4";
                setTimeout(() => {
                    codeElement.style.backgroundColor = "#e0f7fa";
                }, 2000);
            }, function(err) {
                codeElement.style.backgroundColor = "#fab2b2";
                setTimeout(() => {
                    codeElement.style.backgroundColor = "#e0f7fa";
                }, 2000);
            });
        }
    </script>
</head>
<body>
<div class="container">
    <div class="header">
        重置密码成功
    </div>
    <div class="content">
        <p>Hello <%= username %>,</p>
        <p>您的新密码是:</p>
        <a href="<%= newPassword %>" id="newPassword" class="verification-code" onclick="copyToClipboard()">
            <%= newPassword %>
        </a>
        <p>感谢您的使用以及支持。</p>
    </div>
    <div class="footer">
        祝您生活愉快。
    </div>
    <div class="byline">
        发送者 <%= senderName %>
    </div>
</div>
</body>
</html>
